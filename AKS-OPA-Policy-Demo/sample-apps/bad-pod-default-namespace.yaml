# Violates: deny-default-namespace (custom policy, self-authored K8sDenyDefaultNamespace) - deployed to "default"
apiVersion: v1
kind: Pod
metadata:
  name: bad-default-namespace
  namespace: default
  labels:
    team: platform
    environment: dev
spec:
  containers:
    - name: nginx
      image: docker.io/library/nginx:1.27-alpine
      resources:
        limits:
          cpu: "500m"
          memory: "512Mi"
      securityContext:
        readOnlyRootFilesystem: true
